spring:
  application:
    name: "Product"
  datasource:
    url: ${PRODUCT_URL}
    username: ${MYSQL_USERNAME}
    password: ${MYSQL_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

server:
  port: 8086

logging:
  level:
    root: INFO

app:
  security:
    token:
      secret: ${TOKEN_PASSWORD}